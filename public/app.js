const state={cars:[],filter:'all',cart:JSON.parse(localStorage.getItem('ae_cart')||'[]')};
const $=s=>document.querySelector(s);const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
const money=n=>new Intl.NumberFormat('ar-EG').format(Number(n)||0)+' جنيه';
async function load(){try{const r=await fetch('/api/products');if(!r.ok)throw new Error();state.cars=await r.json();render()}catch{$('#grid').innerHTML='<div class="empty">تعذر تحميل السيارات حاليًا. حاول تحديث الصفحة.</div>'}}
function render(){const list=state.cars.filter(c=>state.filter==='all'||c.category===state.filter);if(!list.length){$('#grid').innerHTML='<div class="empty">لا توجد سيارات في هذا القسم حاليًا.</div>';return}$('#grid').innerHTML=list.map(c=>`<article class="card"><div class="card-img"><img src="${esc(c.image)}" alt="${esc(c.name)}" loading="lazy">${c.status==='sold'?'<div class="sold">مباعة</div>':''}</div><div class="card-body"><h3>${esc(c.name)}</h3><div class="meta"><span>${esc(c.year)||'—'}</span><span>${esc(c.transmission)||'—'}</span><span>${esc(c.fuel)||'—'}</span></div><div class="price">${money(c.price)}</div><div class="card-actions"><button class="details" onclick="details('${esc(c.id)}')">التفاصيل</button><button onclick="addCart('${esc(c.id)}')">إضافة لاختياراتي</button></div></div></article>`).join('')}
window.details=id=>{const c=state.cars.find(x=>x.id===id);if(!c)return;$('#modal').classList.remove('hidden');$('#modal').innerHTML=`<div class="modal-card"><div class="modal-head"><div><span class="label">AUTOMOBILE ELKHESHEN</span><h2>${esc(c.name)}</h2><p>${esc(c.description||'للاستفسار عن التفاصيل تواصل معنا مباشرة.')}</p></div><button class="close" onclick="closeModal()">×</button></div><div class="gallery">${(c.images||[c.image]).map(x=>`<img src="${esc(x)}" loading="lazy">`).join('')}</div><div class="meta"><span>السنة: ${esc(c.year)||'—'}</span><span>الفتيس: ${esc(c.transmission)||'—'}</span><span>الوقود: ${esc(c.fuel)||'—'}</span><span>الكيلومترات: ${esc(c.mileage)||'—'}</span><span>اللون: ${esc(c.color)||'—'}</span></div><div class="price">${money(c.price)}</div><a class="primary" href="https://wa.me/201119295523?text=${encodeURIComponent('مرحبًا، أريد الاستفسار عن '+c.name)}" target="_blank">اسأل عن السيارة على واتساب</a></div>`};
window.closeModal=()=>$('#modal').classList.add('hidden');
window.addCart=id=>{if(!state.cart.includes(id))state.cart.push(id);localStorage.setItem('ae_cart',JSON.stringify(state.cart));updateCart()};
function updateCart(){$('#cartCount').textContent=state.cart.length}
function cart(){const cars=state.cart.map(id=>state.cars.find(c=>c.id===id)).filter(Boolean);$('#cartModal').classList.remove('hidden');$('#cartModal').innerHTML=`<div class="modal-card"><div class="modal-head"><div><span class="label">MY SELECTION</span><h2>اختياراتي</h2></div><button class="close" onclick="closeCart()">×</button></div>${cars.length?cars.map(c=>`<div class="cart-row"><img src="${esc(c.image)}"><div><b>${esc(c.name)}</b><div class="meta">${money(c.price)}</div></div><button class="close" onclick="removeCart('${esc(c.id)}')">×</button></div>`).join(''):'<div class="empty">لم تضف أي سيارة بعد.</div>'}<br>${cars.length?`<a class="primary" href="https://wa.me/201119295523?text=${encodeURIComponent('مرحبًا، أريد الاستفسار عن: '+cars.map(c=>c.name).join(' - '))}" target="_blank">إرسال الاختيارات على واتساب</a>`:''}</div>`}
window.removeCart=id=>{state.cart=state.cart.filter(x=>x!==id);localStorage.setItem('ae_cart',JSON.stringify(state.cart));cart();updateCart()};window.closeCart=()=>$('#cartModal').classList.add('hidden');
$('.filters').addEventListener('click',e=>{if(!e.target.matches('button'))return;document.querySelectorAll('.filters button').forEach(b=>b.classList.remove('active'));e.target.classList.add('active');state.filter=e.target.dataset.filter;render()});$('#cartBtn').onclick=cart;$('#menu').onclick=()=>$('#nav').classList.toggle('open');load();updateCart();
